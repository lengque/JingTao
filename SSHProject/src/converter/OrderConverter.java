package converter;

import model.Order;
import model.OrderDTO;

public class OrderConverter {
	/**
	 * 将前台OrderDto对象数据放到User中
	 * 
	 */
	public Order addOrderConverter(OrderDTO orderDto) {
		Order order = new Order();

		return order;
	}
}
